import ChatContainer from "../ChatContainer";
import Stage from "../Stage";

const Town = () => {
  return (
    <div>
      <ChatContainer />
      <Stage />
    </div>
  );
};

export default Town;
